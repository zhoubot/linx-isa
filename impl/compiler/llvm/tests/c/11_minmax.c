long max_i64(long a, long b) { return (a > b) ? a : b; }

long min_i64(long a, long b) { return (a < b) ? a : b; }

unsigned long max_u64(unsigned long a, unsigned long b) {
  return (a > b) ? a : b;
}

unsigned long min_u64(unsigned long a, unsigned long b) {
  return (a < b) ? a : b;
}
