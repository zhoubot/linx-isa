# C++ Models (planned)

This directory will contain C++ reference models (functional model, performance model, etc.).

The decode/encode tables should be generated from `spec/isa/spec/current/linxisa-v0.3.json`.
