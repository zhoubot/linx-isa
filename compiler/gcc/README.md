# GCC (Planned)

This directory is reserved for a future GCC-based LinxISA compiler track.

Current active compiler track:

- Upstream LLVM source mirror: `compiler/llvm/`
- Linx integration/test assets: `compiler/linx-llvm/`
