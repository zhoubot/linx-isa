# Kernel

Kernel integrations are domain-scoped here:

- Linux source mirror (submodule): `kernel/linux/`
- Future OS tracks: `kernel/bsd/`, `kernel/windows/` (placeholders for future bring-up)

LinxISA Linux-on-emulator/RTL bring-up should stay aligned with:

- ISA catalog: `spec/isa/spec/current/linxisa-v0.3.json`
- Bring-up phases: `docs/bringup/phases/`
